# CLI Reference

The Iwa CLI is the primary way to interact with the framework. It is built using `typer`.

## Main Command

::: iwa.core.cli
    handler: python
    options:
        members:
            - iwa_cli
        show_root_heading: true
        show_source: true
